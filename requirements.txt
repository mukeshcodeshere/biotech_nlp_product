pandas==2.0.3
aiohttp==3.8.5
datamule==0.1.4  # Replace with the actual version if known, as datamule may not be a well-known package.
subprocess32==3.5.4  # In case you're using subprocess with compatibility for Python 2.x (not usually needed for Python 3).
